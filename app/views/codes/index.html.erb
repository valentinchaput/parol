

<body>


    <div class="row">
      <div class="col-xs-8 col-xs-offset-2 col-sm-8 col-sm-offset-2">

        <h1>Les codes de loi</h1>
        <br>
        <ul>
          <% @codes.each do |code| %>
          <li>
            <h4><%= code.title %></h4>
            <p><%= code.description %></p>
            <p><%= link_to 'Details', code_path(code), class: 'btn btn-default' %></p>
          </li>
          <% end %>
        </ul>

    </div>
  </div>
</body>


<!--           <div class="dropdown">
            <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu" data-toggle="dropdown" aria-expanded="true">
              Pick a law
              <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu" id="dropdownMenu">
              <% Law.all.each do |law| %>
              <li role="presentation">
                <%= link_to law.title, law_path(law), role: "menuitem" %>
              </li>
              <% end %>
            </ul>
          </div>

        </div>
 -->
